{
  "name": "aichanting-server",
  "version": "1.0.0",
  "description": "赛博功德转换器后端服务",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "reset": "node reset-db.js",
    "test": "node --check server.js && node --check db.js && node --check routes/merit.js && node --check routes/ranking.js && node --check middleware/rateLimit.js && node --check reset-db.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0",
    "express": "^4.19.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
